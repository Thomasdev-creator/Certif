{{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }} 
    {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button> <!-- J'ai ajouté la classe "btn-primary" pour un bouton de sauvegarde -->
{{ form_end(form) }}
