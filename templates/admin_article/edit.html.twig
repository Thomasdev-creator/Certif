{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <div class="container mt-5"> <!-- Container pour centrer et ajouter des marges autour de votre contenu -->
        <h1>Edit Article</h1>

        {{ include('admin_article/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('app_admin_article_index') }}" class="btn btn-secondary mt-2">back to list</a> <!-- Bouton Bootstrap -->

        <div class="mt-3">
            {{ include('admin_article/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
